def displayRange(lower, upper):
    """Outputs the numbers from lower through upper."""
    if lower <= upper:
        print(lower)
        displayRange(lower + 1, upper)

# Call the function to output the numbers from 1 to 5
displayRange(1, 5)
